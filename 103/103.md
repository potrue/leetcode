## 何も見ずに解いてみる

```python
class Solution:
    def zigzagLevelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        if not root:
            return []
        result = []
        left_to_right = True
        nodes = [root]
        while nodes:
            values = []
            next_nodes = []
            for node in nodes:
                values.append(node.val)
                if left_to_right:
                    if node.left:
                        next_nodes.append(node.left)
                    if node.right:
                        next_nodes.append(node.right)
                else:
                    if node.right:
                        next_nodes.append(node.right)
                    if node.left:
                        next_nodes.append(node.left)
            result.append(values)
            left_to_right = not left_to_right
            nodes = list(reversed(next_nodes))
        return result
```

## 色々調べてみる

https://github.com/Satorien/LeetCode/pull/27  
https://github.com/ryosuketc/leetcode_arai60/pull/27  
https://github.com/tokuhirat/LeetCode/pull/27  
https://github.com/olsen-blue/Arai60/pull/27  

valuesに入れるときにだけreverseをする必要があれば行い、ノードのチェック自体は左から右に行っていくというやり方の人が多かった。  
自分の最初のコードは左から見るか右から見るかを変数によって変える必要があるので、少しロジックをとらえづらいかもしれない。

## 最終コード

```python
class Solution:
    def zigzagLevelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        if not root:
            return []
        result = []
        on_odd_level = False
        nodes = [root]
        while nodes:
            values = []
            result.append(values)
            next_nodes = []
            for node in nodes:
                values.append(node.val)
                if node.left:
                    next_nodes.append(node.left)
                if node.right:
                    next_nodes.append(node.right)
            if on_odd_level:
                values.reverse()
            on_odd_level = not on_odd_level
            nodes = next_nodes
        return result
```
